name: Build OpenMRS Frontend Module

on:
  workflow_call:
    inputs:
      node_version:
        description: 'Node.js version to use. If omitted, inferred from engines.node in package.json.'
        type: string
      build_command:
        description: 'Command to build the project. If omitted, inferred from package.json.'
        type: string
        default: ''
      verify_command:
        description: 'Command to run lint, type checks, and tests. If omitted, inferred from package.json.'
        type: string
        default: ''
      pre_verify_command:
        description: 'Optional command to run before verify (e.g., yarn check-translations). Skipped if empty.'
        type: string
        default: ''
      enable_turborepo_cache:
        description: >-
          Enable Turborepo remote caching, backed by the GitHub Actions cache.
          If omitted, inferred (true when turbo.json is present).
        type: string
        default: ''
      artifact_path:
        description: 'Path(s) to build artifacts to upload (glob supported). If omitted, inferred from package.json.'
        type: string
        default: ''

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Checkout inference action
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: openmrs/openmrs-contrib-gha-workflows
          ref: ${{ github.job_workflow_sha }}
          sparse-checkout: |
            .github/actions
            .github/scripts
          sparse-checkout-cone-mode: false
          path: .gha-workflows

      - name: Infer frontend parameters
        id: params
        uses: ./.gha-workflows/.github/actions/infer-frontend-params
        with:
          node_version: ${{ inputs.node_version }}
          build_command: ${{ inputs.build_command }}
          verify_command: ${{ inputs.verify_command }}
          enable_turborepo_cache: ${{ inputs.enable_turborepo_cache }}
          artifact_path: ${{ inputs.artifact_path }}

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ steps.params.outputs.node_version }}
          cache: 'yarn'

      - name: 🗂️ Cache node_modules
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: '**/node_modules'
          key: ${{ runner.os }}-node-${{ steps.params.outputs.node_version }}-modules-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ steps.params.outputs.node_version }}-modules-

      - name: 📦 Install dependencies
        run: yarn install --immutable

      - name: 🚀 Setup Turborepo cache
        if: steps.params.outputs.enable_turborepo_cache == 'true'
        uses: rharkor/caching-for-turbo@2238fae6eb9a9936f92356f54cb3660200d105e7 # v2.5.1

      - name: 🏗️ Run build
        run: ${{ steps.params.outputs.build_command }}

      - name: 🔍 Run pre-verify command
        if: inputs.pre_verify_command != ''
        run: ${{ inputs.pre_verify_command }}

      - name: 🔍 Run lint, type checks and tests
        run: ${{ steps.params.outputs.verify_command }}

      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ github.event.repository.name }}-packages
          path: ${{ steps.params.outputs.artifact_path }}